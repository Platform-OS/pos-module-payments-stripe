{% liquid
  # TODO: need to store this request in gateway_requests
  graphql response = 'modules/payments_stripe/api_call', template: "modules/payments_stripe/checkout_session_create", data: object

  function object = 'modules/payments_stripe/commands/stripe_checkout/create/map_response', object: response.api_call.response

  return object
%}
