{% liquid
  assign indempotency_key = 20 | random_string
  assign to = 'https://api.stripe.com/v1/webhook_endpoints/' | append: gateway_id

  assign object = null | hash_merge: to: to, request_type: 'Delete', indempotency_key: indempotency_key, gateway_id: gateway_id
  return object
%}
