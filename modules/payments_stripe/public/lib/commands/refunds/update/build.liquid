{%- parse_json object -%}
  {
    "id": {{ object.id | json }},
    "refund_id": {{ params.refund_id | default: object.refund_id | json }},
    "status": {{ params.status | json }}
  }
{%- endparse_json -%}
{% liquid
  hash_assign object['gateway_errors'] = params.errors | json
  return object
%}
